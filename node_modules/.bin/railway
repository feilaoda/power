#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../railway/bin/railway" "$@"
  ret=$?
else 
  node  "$basedir/../railway/bin/railway" "$@"
  ret=$?
fi
exit $ret
