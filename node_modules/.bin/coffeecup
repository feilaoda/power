#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../coffeecup/bin/coffeecup" "$@"
  ret=$?
else 
  node  "$basedir/../coffeecup/bin/coffeecup" "$@"
  ret=$?
fi
exit $ret
